{
  "binary": "/Users/keren/.local/bin/ibkr-cli",
  "description": "List the account's recent executions (~last 7 days, read-only, via the local Client Portal Gateway).",
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "account": {
        "type": "string",
        "description": "Account id. Omit to use the resolved primary account."
      },
      "output": {
        "type": "string",
        "enum": ["json", "ndjson", "table"],
        "description": "Output framing. Defaults to json."
      }
    }
  },
  "argv": ["trades", "--account={account}", "--output={output}"],
  "limits": { "timeoutMs": 30000, "maxOutputBytes": 524288 }
}
