name: cobblecompanion-gcp
runtime: nodejs
description: GCP project configuration for CobbleCompanion — a single Cloud Run service (Fastify API + built SPA), Artifact Registry, Secret Manager, per-service IAM.
