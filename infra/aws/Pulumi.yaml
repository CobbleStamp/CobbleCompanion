name: cobblecompanion-aws
runtime: nodejs
description: CobbleCompanion AWS deployment — a single EC2 t3.micro running the Fastify API + built SPA behind Caddy, with Postgres on Supabase (external). See docs/infra-setup.md.
